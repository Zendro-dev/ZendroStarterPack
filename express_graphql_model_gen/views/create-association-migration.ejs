'use strict';

module.exports = {

    up: function(queryInterface, Sequelize) {
        return queryInterface.addColumn('<%= source %>', '<%= targetKey %>',{
               type: Sequelize.INTEGER,
               references: {
                 model: '<%=target_pl.toLowerCase() %>',
                 key: 'id'
               }
        });
    },

    down: function(queryInterface, Sequelize) {
      return queryInterface.removeColumn('<%= source %>', '<%= targetKey %>');
    }

};
